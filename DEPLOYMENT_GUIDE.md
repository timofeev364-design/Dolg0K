# Как загрузить проект в Telegram Mini App

Telegram Mini App — это просто **веб-сайт**, который открывается внутри Telegram. Чтобы он заработал, вам нужно разместить (захостить) две части вашего приложения в интернете:

1.  **Frontend (Приложение)** — то, что видит пользователь.
2.  **Backend (Сервер API)** — то, где хранятся данные пользователей (`apps/api`).

Ниже простая инструкция, как сделать это бесплатно и быстро.

---

## Шаг 1: Подготовка к Интернету

Ваше приложение сейчас настроено на `localhost` (ваш компьютер). Для работы в интернете нужно поменять адрес сервера в коде.

1.  Вам нужно будет узнать адрес вашего будущего сервера (см. Шаг 3).
2.  Когда узнаете URL сервера (например, `https://babki-api.onrender.com`), нужно будет зайти в `apps/mobile/src/services/api.ts` и поменять:
    ```typescript
    // Было
    const API_URL = isDev ? ... : 'http://localhost:3001';
    
    // Станет (пример)
    const API_URL = 'https://ваш-адрес-сервера.com';
    ```

---

## Шаг 2: Frontend (Визуальная часть) -> Vercel

Самый простой способ загрузить сайт (Expo Web) — использовать **Vercel**.

1.  Зарегистрируйтесь на [vercel.com](https://vercel.com) (можно через GitHub).
2.  Установите Vercel CLI (в терминале):
    ```bash
    npm install -g vercel
    ```
3.  В терминале перейдите в папку проекта:
    ```bash
    cd apps/mobile
    ```
4.  Запустите команду деплоя:
    ```bash
    npx vercel
    ```
5.  Отвечайте на вопросы (везде можно жать Enter / Yes).
6.  В конце Vercel выдаст вам ссылку вида: `https://babki-mobile.vercel.app`. **Сохраните её.**

*(Примечание: Возможно потребуется собрать статический билд командой `npx expo export:web` и указывать папку `dist` при деплое, если прямой деплой потребует настроек).*

---

## Шаг 3: Backend (Сервер API) -> Render.com

Так как у нас простой Node.js сервер с файлом базы данных, нам нужен хостинг, который поддерживает работу Node.js. **Render** подойдет (бесплатный тариф может "засыпать", но для тестов хватит).

1.  Зарегистрируйтесь на [render.com](https://render.com).
2.  Создайте "Web Service".
3.  Подключите ваш GitHub репозиторий (если код там).
    *   *Если вы не используете GitHub, можно использовать `glitch.com` или пробросить туннель через `ngrok` прямо с вашего компьютера для тестов.*
4.  Команда старта: `npm run api` (или `node apps/api/server.js`).
5.  После деплоя Render даст вам адрес (например, `https://babki-api.onrender.com`).
6.  **ВАЖНО**: Вернитесь в код (Шаг 1), вставьте этот адрес в `api.ts`, и **заново обновите Frontend на Vercel** (команда `npx vercel --prod`), чтобы он узнал о новом сервере.

---

## Шаг 4: Главный Босс -> @BotFather

Теперь подключим всё это к Telegram.

1.  Откройте Telegram, найдите бота **@BotFather**.
2.  Напишите `/newbot`, придумайте имя и юзернейм для бота (например, `babki_finance_bot`).
3.  Бот создан. Теперь привяжем "кнопку меню" к вашему сайту.
4.  Напишите `/mybots` -> Выберите вашего бота.
5.  Выберите **Bot Settings** -> **Menu Button** -> **Configure Menu Button**.
6.  Отправьте ссылку на ваш **Frontend** (из Шага 2, например `https://babki-mobile.vercel.app`).
7.  Дайте название кнопке (например, "Запустить Babki").

## Шаг 5: Проверка

1.  Зайдите в своего бота.
2.  Нажмите кнопку "Меню" или "Запустить Babki".
3.  Ваше приложение откроется прямо внутри Telegram! 🎉

---

### 🚀 Быстрый способ (для тестов прямо сейчас без хостинга)

Если вы хотите просто показать другу или проверить за 5 минут:

1.  Скачайте **ngrok** (https://ngrok.com).
2.  Запустите сервер и сайт локально (`npm run api` и `npm run mobile:web`).
3.  В терминале запустите ngrok для сайта:
    ```bash
    ngrok http 8081
    ```
4.  Ngrok даст ссылку `https://xxxx-xxxx.ngrok-free.app`.
5.  Вставьте эту ссылку в BotFather (Шаг 4).
6.  **НО**: Сервер API тоже должен быть доступен. Если приложение делает запросы к `localhost`, на телефоне это не сработает. Вариант: не использовать API для тестов, или туннелировать и его тоже.
